- name: vars from file
  hosts: local
  connection: local
  vars_files:
    - course.yaml
  tasks:
  - name: vbars from file
    ansible.builtin.debug:
      msg: "course is {{course}},trainer is {{trainer}} and duration is {{duration}}"